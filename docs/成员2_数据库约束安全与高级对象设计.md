# 乒乓球运动员综合训练监控管理系统 · 成员 2 数据库约束安全与高级对象设计

> 成员 2 | 数据库约束、安全、索引、视图、存储过程、触发器补充设计 | 2026-07-06  
> 本地修订 | 2026-07-09：修复 `sp_refresh` 空值问题、补充软删除字段与完整数据字典

---

## 一、任务范围

本文件基于成员 1 提交的 `sql/pingpang_db.sql` 和 `docs/数据库设计交接说明.md` 继续补充数据库设计，重点完成以下内容：

- 主键、外键、非空、唯一约束说明
- 完整数据字典（逐表逐字段）
- 查询索引设计与设计理由
- 伤病表软删除字段增强（供成员 8 对接）
- 数据库权限账号补充
- 备份与恢复方案
- 视图、存储过程、触发器等高级数据库对象

对应 SQL 交付文件：

```text
sql/member2_advanced_database.sql
```

该脚本依赖成员 1 的建库建表脚本，执行顺序如下：

```powershell
mysql -u root -p < sql/pingpang_db.sql
mysql -u root -p pingpang_db < sql/member2_advanced_database.sql
```

> 注意：`.env` / `.env.example` 中的 `DB_NAME` 必须为 `pingpang_db`，不要写成 `table_tennis_training`。

---

## 二、数据字典

### 2.1 核心表一览

| 表名 | 中文含义 | 主要作用 |
|---|---|---|
| `athlete` | 运动员 | 保存运动员基础档案和伤病状态 |
| `coach` | 教练员 | 保存教练基础资料 |
| `training_plan` | 训练计划 | 记录运动员训练安排 |
| `technical_record` | 专项技术记录 | 记录正手、反手、发球、步法等技术评分 |
| `fitness_report` | 体能测试报告 | 记录力量、柔韧、耐力、速度等体能指标 |
| `injury_record` | 伤病记录 | 保存伤病登记、治疗、康复状态及软删除信息 |
| `match_record` | 比赛成绩 | 记录比赛名称、对手、结果和比分 |
| `user_account` | 用户账号 | 支持登录和角色权限控制 |

### 2.2 `athlete` 运动员

| 字段名 | 类型 | 可空 | 默认值 | 约束 | 含义 | 取值说明 |
|---|---|---|---|---|---|---|
| `id` | `INT` | 否 | 自增 | 主键 | 运动员内部编号 | — |
| `student_no` | `VARCHAR(20)` | 否 | — | 唯一 | 学号/队内编号 | 业务唯一身份 |
| `name` | `VARCHAR(50)` | 否 | — | — | 姓名 | — |
| `gender` | `ENUM` | 否 | — | — | 性别 | `男` / `女` |
| `birth_date` | `DATE` | 是 | NULL | — | 出生日期 | 年龄由应用层计算 |
| `team` | `VARCHAR(100)` | 是 | NULL | — | 所属队伍 | 如校队、省青年队 |
| `skill_level` | `ENUM` | 否 | — | — | 运动等级 | `二级运动员`/`一级运动员`/`国家级`/`健将级`/`青年队` |
| `play_style` | `VARCHAR(100)` | 是 | NULL | — | 打法风格 | 如快攻结合弧圈 |
| `grip` | `VARCHAR(50)` | 是 | NULL | — | 握拍方式 | 如右手横板 |
| `contact_phone` | `VARCHAR(20)` | 是 | NULL | — | 联系电话 | — |
| `injury_status` | `ENUM` | 否 | `健康` | — | 当前健康状态 | `健康`/`观察中`/`康复中`/`伤病中`，由触发器维护 |
| `create_time` | `TIMESTAMP` | 是 | `CURRENT_TIMESTAMP` | — | 创建时间 | — |
| `update_time` | `TIMESTAMP` | 是 | NULL | — | 更新时间 | — |

### 2.3 `coach` 教练员

| 字段名 | 类型 | 可空 | 默认值 | 约束 | 含义 | 取值说明 |
|---|---|---|---|---|---|---|
| `id` | `INT` | 否 | 自增 | 主键 | 教练内部编号 | — |
| `name` | `VARCHAR(50)` | 否 | — | — | 姓名 | — |
| `gender` | `ENUM` | 是 | NULL | — | 性别 | `男` / `女` |
| `specialty` | `VARCHAR(100)` | 是 | NULL | — | 擅长方向 | 技术/体能/战术等 |
| `contact_phone` | `VARCHAR(20)` | 是 | NULL | — | 联系电话 | — |
| `email` | `VARCHAR(100)` | 是 | NULL | 唯一 | 邮箱 | 允许为空，非空时唯一 |
| `create_time` | `TIMESTAMP` | 是 | `CURRENT_TIMESTAMP` | — | 创建时间 | — |

### 2.4 `training_plan` 训练计划

| 字段名 | 类型 | 可空 | 默认值 | 约束 | 含义 | 取值说明 |
|---|---|---|---|---|---|---|
| `id` | `INT` | 否 | 自增 | 主键 | 计划编号 | — |
| `athlete_id` | `INT` | 否 | — | 外键→athlete | 运动员 | 删除运动员时 CASCADE |
| `coach_id` | `INT` | 否 | — | 外键→coach | 负责教练 | 有计划时教练不可删（RESTRICT） |
| `plan_name` | `VARCHAR(100)` | 否 | — | — | 计划名称 | — |
| `start_date` | `DATE` | 否 | — | — | 开始日期 | 应用层应保证 ≤ end_date |
| `end_date` | `DATE` | 否 | — | — | 结束日期 | — |
| `training_content` | `TEXT` | 是 | NULL | — | 训练内容 | — |
| `intensity` | `ENUM` | 否 | — | — | 训练强度 | `低`/`中`/`高`/`极高` |
| `hours` | `DECIMAL(4,1)` | 是 | NULL | — | 计划课时 | 单位：小时 |
| `status` | `ENUM` | 是 | `进行中` | — | 计划状态 | `进行中`/`已完成`/`已取消` |
| `create_time` | `TIMESTAMP` | 是 | `CURRENT_TIMESTAMP` | — | 创建时间 | — |
| `update_time` | `TIMESTAMP` | 是 | NULL | — | 更新时间 | — |

### 2.5 `technical_record` 专项技术记录

| 字段名 | 类型 | 可空 | 默认值 | 约束 | 含义 | 取值说明 |
|---|---|---|---|---|---|---|
| `id` | `INT` | 否 | 自增 | 主键 | 记录编号 | — |
| `athlete_id` | `INT` | 否 | — | 外键→athlete | 运动员 | CASCADE |
| `record_date` | `DATE` | 否 | — | — | 测评日期 | — |
| `evaluator_id` | `INT` | 是 | NULL | 外键→coach | 评估教练 | 教练删除时 SET NULL |
| `forehand_score` | `DECIMAL(5,2)` | 是 | NULL | — | 正手得分 | 建议 0–100，应用层校验 |
| `backhand_score` | `DECIMAL(5,2)` | 是 | NULL | — | 反手得分 | 同上 |
| `serve_score` | `DECIMAL(5,2)` | 是 | NULL | — | 发球得分 | 同上 |
| `footwork_score` | `DECIMAL(5,2)` | 是 | NULL | — | 步法得分 | 同上 |
| `reaction_score` | `DECIMAL(5,2)` | 是 | NULL | — | 反应得分 | 同上 |
| `overall_score` | `DECIMAL(5,2)` | 是 | NULL | — | 综合得分 | 可取五项平均 |
| `notes` | `TEXT` | 是 | NULL | — | 备注 | — |
| `create_time` | `TIMESTAMP` | 是 | `CURRENT_TIMESTAMP` | — | 创建时间 | — |

### 2.6 `fitness_report` 体能测试报告

| 字段名 | 类型 | 可空 | 默认值 | 约束 | 含义 | 取值说明 |
|---|---|---|---|---|---|---|
| `id` | `INT` | 否 | 自增 | 主键 | 报告编号 | — |
| `athlete_id` | `INT` | 否 | — | 外键→athlete | 运动员 | CASCADE |
| `test_date` | `DATE` | 否 | — | — | 测试日期 | — |
| `tester_id` | `INT` | 是 | NULL | 外键→coach | 测试教练 | SET NULL |
| `upper_strength` | `DECIMAL(5,2)` | 是 | NULL | — | 上肢力量 | 建议 0–100 |
| `lower_strength` | `DECIMAL(5,2)` | 是 | NULL | — | 下肢力量 | 同上 |
| `flexibility` | `DECIMAL(5,2)` | 是 | NULL | — | 柔韧性 | 同上 |
| `endurance` | `DECIMAL(5,2)` | 是 | NULL | — | 耐力 | 同上 |
| `speed` | `DECIMAL(5,2)` | 是 | NULL | — | 速度 | 同上 |
| `overall_score` | `DECIMAL(5,2)` | 是 | NULL | — | 综合得分 | — |
| `notes` | `TEXT` | 是 | NULL | — | 备注 | — |
| `create_time` | `TIMESTAMP` | 是 | `CURRENT_TIMESTAMP` | — | 创建时间 | — |

### 2.7 `injury_record` 伤病记录

| 字段名 | 类型 | 可空 | 默认值 | 约束 | 含义 | 取值说明 |
|---|---|---|---|---|---|---|
| `id` | `INT` | 否 | 自增 | 主键 | 记录编号 | — |
| `athlete_id` | `INT` | 否 | — | 外键→athlete | 运动员 | CASCADE |
| `injury_date` | `DATE` | 否 | — | — | 受伤日期 | — |
| `injury_location` | `VARCHAR(100)` | 否 | — | — | 受伤部位 | 如右肩、左膝 |
| `injury_type` | `VARCHAR(100)` | 是 | NULL | — | 伤病类型 | 如肌肉拉伤 |
| `severity` | `ENUM` | 否 | — | — | 严重程度 | `轻微`/`中度`/`严重` |
| `diagnosis` | `TEXT` | 是 | NULL | — | 诊断说明 | — |
| `treatment` | `TEXT` | 是 | NULL | — | 治疗方案 | — |
| `recovery_status` | `ENUM` | 是 | `治疗中` | — | 恢复状态 | `治疗中`/`康复中`/`已恢复` |
| `expected_recovery_date` | `DATE` | 是 | NULL | — | 预计恢复日期 | 应用层应 ≥ injury_date |
| `notes` | `TEXT` | 是 | NULL | — | 备注 | — |
| `is_deleted` | `TINYINT(1)` | 否 | `0` | — | 是否作废 | `0` 有效 / `1` 已软删除（成员 2 补充） |
| `deleted_at` | `DATETIME` | 是 | NULL | — | 作废时间 | 软删除时写入 |
| `deleted_by` | `VARCHAR(50)` | 是 | NULL | — | 作废操作人 | 用户名或角色 |
| `delete_reason` | `VARCHAR(200)` | 是 | NULL | — | 作废原因 | — |
| `create_time` | `TIMESTAMP` | 是 | `CURRENT_TIMESTAMP` | — | 创建时间 | — |
| `update_time` | `TIMESTAMP` | 是 | NULL | — | 更新时间 | — |

### 2.8 `match_record` 比赛成绩

| 字段名 | 类型 | 可空 | 默认值 | 约束 | 含义 | 取值说明 |
|---|---|---|---|---|---|---|
| `id` | `INT` | 否 | 自增 | 主键 | 记录编号 | — |
| `athlete_id` | `INT` | 否 | — | 外键→athlete | 运动员 | CASCADE |
| `match_date` | `DATE` | 否 | — | — | 比赛日期 | — |
| `match_name` | `VARCHAR(100)` | 否 | — | — | 比赛名称 | — |
| `opponent` | `VARCHAR(100)` | 是 | NULL | — | 对手 | — |
| `result` | `ENUM` | 否 | — | — | 赛果 | `胜`/`负`/`平` |
| `score` | `VARCHAR(50)` | 是 | NULL | — | 比分 | 如 `3:1` |
| `notes` | `TEXT` | 是 | NULL | — | 备注 | — |
| `create_time` | `TIMESTAMP` | 是 | `CURRENT_TIMESTAMP` | — | 创建时间 | — |

### 2.9 `user_account` 用户账号

| 字段名 | 类型 | 可空 | 默认值 | 约束 | 含义 | 取值说明 |
|---|---|---|---|---|---|---|
| `id` | `INT` | 否 | 自增 | 主键 | 账号编号 | — |
| `username` | `VARCHAR(50)` | 否 | — | 唯一 | 登录名 | — |
| `password_hash` | `VARCHAR(255)` | 否 | — | — | 密码哈希 | 使用 scrypt 等，禁止明文 |
| `role` | `ENUM` | 否 | — | — | 角色 | `管理员`/`教练员` |
| `coach_id` | `INT` | 是 | NULL | 唯一、外键→coach | 绑定教练 | 一个教练最多一个账号 |
| `create_time` | `TIMESTAMP` | 是 | `CURRENT_TIMESTAMP` | — | 创建时间 | — |

### 2.10 视图字段说明

#### `v_athlete_comprehensive_profile`

| 字段 | 含义 |
|---|---|
| `athlete_id` / `student_no` / `name` / `gender` / `team` | 运动员基础信息 |
| `skill_level` / `play_style` / `grip` / `injury_status` | 等级、打法与健康状态 |
| `training_plan_count` | 训练计划数量 |
| `technical_record_count` / `avg_technical_score` | 技术记录数与平均综合分 |
| `fitness_report_count` / `avg_fitness_score` | 体测报告数与平均综合分 |
| `injury_record_count` | 有效伤病记录数（排除 `is_deleted=1`） |
| `active_injury_count` | 当前未恢复伤病数 |
| `match_count` / `win_count` | 比赛场次与胜场 |

#### `v_monthly_training_summary`

| 字段 | 含义 |
|---|---|
| `athlete_id` / `student_no` / `athlete_name` | 运动员标识 |
| `training_month` | 月份，格式 `YYYY-MM`（来自技术记录日期） |
| `technical_record_count` | 当月技术测评次数 |
| `avg_forehand_score` 等 | 正手/反手/发球/步法/反应/综合平均分 |

### 2.11 存储过程参数说明

| 过程名 | 参数 | 类型 | 说明 |
|---|---|---|---|
| `sp_filter_athletes_by_level` | `p_skill_level` | `VARCHAR(20)` | 运动等级；空或 NULL 返回全部 |
| `sp_get_monthly_training_summary` | `p_training_month` | `VARCHAR(7)` | `YYYY-MM`；空或 NULL 返回全部月份 |
| `sp_refresh_athlete_injury_status` | `p_athlete_id` | `INT` | 要刷新状态的运动员 id |

---

## 三、约束设计说明

### 3.1 主键约束

所有核心业务表均采用 `id INT AUTO_INCREMENT PRIMARY KEY` 作为主键。该设计保证每条记录具有稳定、唯一的内部标识，便于外键引用和后端接口传参。

涉及表：

```text
athlete, coach, training_plan, technical_record,
fitness_report, injury_record, user_account, match_record
```

### 3.2 外键约束

本系统使用 InnoDB 引擎维护外键完整性，避免出现训练、体测、伤病、比赛数据找不到对应运动员的情况。

| 子表 | 外键字段 | 父表 | 删除策略 | 设计理由 |
|---|---|---|---|---|
| `training_plan` | `athlete_id` | `athlete(id)` | `CASCADE` | 删除运动员时同步清理其训练计划 |
| `training_plan` | `coach_id` | `coach(id)` | `RESTRICT` | 教练有训练计划时不允许删除 |
| `technical_record` | `athlete_id` | `athlete(id)` | `CASCADE` | 删除运动员时清理技术记录 |
| `technical_record` | `evaluator_id` | `coach(id)` | `SET NULL` | 教练离职后历史评分保留 |
| `fitness_report` | `athlete_id` | `athlete(id)` | `CASCADE` | 删除运动员时清理体测记录 |
| `fitness_report` | `tester_id` | `coach(id)` | `SET NULL` | 教练离职后历史体测保留 |
| `injury_record` | `athlete_id` | `athlete(id)` | `CASCADE` | 删除运动员时清理伤病记录 |
| `match_record` | `athlete_id` | `athlete(id)` | `CASCADE` | 删除运动员时清理比赛记录 |
| `user_account` | `coach_id` | `coach(id)` | `SET NULL` | 账号注销不影响教练档案 |

### 3.3 非空约束

核心业务字段必须设置为 `NOT NULL`，避免出现无法解释的业务数据。

主要包括：

- 运动员：`student_no`, `name`, `gender`, `skill_level`, `injury_status`
- 训练计划：`athlete_id`, `coach_id`, `plan_name`, `start_date`, `end_date`, `intensity`
- 专项技术记录：`athlete_id`, `record_date`
- 体能测试报告：`athlete_id`, `test_date`
- 伤病记录：`athlete_id`, `injury_date`, `injury_location`, `severity`, `is_deleted`
- 比赛记录：`athlete_id`, `match_date`, `match_name`, `result`
- 用户账号：`username`, `password_hash`, `role`

### 3.4 唯一约束

| 表 | 字段 | 说明 |
|---|---|---|
| `athlete` | `student_no` | 防止重复录入同一运动员 |
| `coach` | `email` | 防止同一邮箱绑定多个教练 |
| `user_account` | `username` | 登录账号必须唯一 |
| `user_account` | `coach_id` | 一个教练最多绑定一个系统账号 |

### 3.5 默认值约束

| 表 | 字段 | 默认值 | 说明 |
|---|---|---|---|
| `athlete` | `injury_status` | `健康` | 新增运动员默认健康 |
| `training_plan` | `status` | `进行中` | 新计划默认进行中 |
| `injury_record` | `recovery_status` | `治疗中` | 新伤病记录默认治疗中 |
| `injury_record` | `is_deleted` | `0` | 新记录默认有效 |
| 多数表 | `create_time` | `CURRENT_TIMESTAMP` | 自动记录创建时间 |

### 3.6 业务约束说明（MySQL 5.5）

本项目目标环境为 MySQL 5.5，`CHECK` 约束不会被强制执行。以下规则由**应用层校验**保证，并在文档中明确：

| 规则 | 建议实现位置 |
|---|---|
| 技术/体测评分 ∈ [0, 100] | Flask 表单校验 |
| `end_date >= start_date` | 训练计划保存逻辑 |
| `expected_recovery_date >= injury_date` | 伤病模块（已有测试覆盖） |
| 软删除后不参与健康状态回算 | `sp_refresh_athlete_injury_status` |

### 3.7 软删除字段增强（成员 2 补充）

在 `sql/member2_advanced_database.sql` 第零部分执行：

```sql
ALTER TABLE injury_record
    ADD COLUMN is_deleted TINYINT(1) NOT NULL DEFAULT 0,
    ADD COLUMN deleted_at DATETIME NULL,
    ADD COLUMN deleted_by VARCHAR(50) NULL,
    ADD COLUMN delete_reason VARCHAR(200) NULL;
```

作用：

- 对齐成员 8 伤病作废/归档逻辑，避免扩展脚本因缺列失败
- 触发器在 `UPDATE is_deleted=1` 时会自动回算 `athlete.injury_status`

---

## 四、索引设计

索引设计文件位于 `sql/member2_advanced_database.sql`。设计原则是：只围绕高频查询条件、排序字段和统计分组字段加索引，避免盲目增加维护成本。

| 索引名 | 表 | 字段 | 支持场景 |
|---|---|---|---|
| `idx_athlete_name` | `athlete` | `name` | 按姓名查询运动员 |
| `idx_athlete_level_status` | `athlete` | `skill_level`, `injury_status` | 按等级和健康状态筛选 |
| `idx_training_plan_athlete_date` | `training_plan` | `athlete_id`, `start_date`, `end_date` | 查询运动员日期区间训练计划 |
| `idx_training_plan_coach_status` | `training_plan` | `coach_id`, `status` | 教练查看自己负责的计划 |
| `idx_technical_record_athlete_date` | `technical_record` | `athlete_id`, `record_date` | 查询运动员技术训练历史 |
| `idx_technical_record_evaluator_date` | `technical_record` | `evaluator_id`, `record_date` | 查询教练评估记录 |
| `idx_fitness_report_athlete_date` | `fitness_report` | `athlete_id`, `test_date` | 查询运动员体测历史 |
| `idx_fitness_report_tester_date` | `fitness_report` | `tester_id`, `test_date` | 查询教练体测记录 |
| `idx_injury_record_athlete_status` | `injury_record` | `athlete_id`, `recovery_status` | 查询运动员当前伤病 |
| `idx_injury_record_recovery_date` | `injury_record` | `recovery_status`, `expected_recovery_date` | 康复跟踪和预警排序 |
| `idx_injury_record_active` | `injury_record` | `athlete_id`, `is_deleted`, `recovery_status` | 有效伤病列表与状态回算 |
| `idx_match_record_athlete_date` | `match_record` | `athlete_id`, `match_date` | 查询运动员比赛历史 |
| `idx_match_record_result_date` | `match_record` | `result`, `match_date` | 统计胜负结果 |

---

## 五、视图设计

### 5.1 `v_athlete_comprehensive_profile`

该视图整合运动员基础信息、训练计划数量、技术记录数量、平均技术分、体测记录数量、平均体测分、伤病数量和比赛胜场数。伤病相关计数已排除 `is_deleted=1` 的记录。

适用场景：

- 运动员综合档案页
- 教练查看运动员训练概况
- 统计分析大屏
- 答辩演示快速查询

示例：

```sql
SELECT * FROM v_athlete_comprehensive_profile;
```

### 5.2 `v_monthly_training_summary`

该视图按运动员和月份汇总**专项技术记录**（`technical_record`），输出正手、反手、发球、步法、反应和综合分平均值。  
命名中的 “training” 指技术训练测评汇总，不是 `training_plan` 表。

适用场景：

- 月度训练报告
- ECharts 折线图/柱状图数据源
- 运动员技术成长趋势分析

示例：

```sql
SELECT * FROM v_monthly_training_summary
WHERE training_month = '2026-06';
```

---

## 六、存储过程设计

### 6.1 `sp_filter_athletes_by_level`

按运动等级筛选运动员，参数为空时返回全部运动员。

```sql
CALL sp_filter_athletes_by_level('一级运动员');
CALL sp_filter_athletes_by_level('');
```

### 6.2 `sp_get_monthly_training_summary`

按月份查询训练技术汇总，参数格式为 `YYYY-MM`。

```sql
CALL sp_get_monthly_training_summary('2026-06');
CALL sp_get_monthly_training_summary('');
```

### 6.3 `sp_refresh_athlete_injury_status`

根据 `injury_record` 中**未软删除且未恢复**的伤病记录，自动刷新 `athlete.injury_status`。

状态规则：

| 条件 | athlete.injury_status |
|---|---|
| 存在 `治疗中` 且 `严重` 的伤病 | `伤病中` |
| 存在 `治疗中` 的伤病 | `观察中` |
| 存在 `康复中` 的伤病 | `康复中` |
| 没有未恢复伤病（含全部已恢复/已作废） | `健康` |

**2026-07-09 修复：**

- 使用局部变量 + `IFNULL(v_status, '健康')`，避免无活跃伤病时把 `NOT NULL` 的 `injury_status` 写成 `NULL`
- 回算时增加 `is_deleted = 0` 条件，作废记录不参与状态计算

该过程主要由触发器自动调用，也可以手动修复单个运动员状态：

```sql
CALL sp_refresh_athlete_injury_status(1);
```

无活跃伤病时的验证：

```sql
-- 将某运动员全部伤病标为已恢复或作废后
CALL sp_refresh_athlete_injury_status(1);
SELECT id, name, injury_status FROM athlete WHERE id = 1;
-- 预期: injury_status = '健康'
```

---

## 七、触发器设计

触发器用于保证 `athlete.injury_status` 与 `injury_record.recovery_status` 保持一致。

| 触发器 | 触发时机 | 作用 |
|---|---|---|
| `trg_injury_after_insert` | 新增伤病记录后 | 刷新该运动员伤病状态 |
| `trg_injury_after_update` | 修改伤病记录后 | 刷新新旧运动员伤病状态（含软删除） |
| `trg_injury_after_delete` | 物理删除伤病记录后 | 刷新该运动员伤病状态 |

测试示例：

```sql
INSERT INTO injury_record (
    athlete_id, injury_date, injury_location, severity, recovery_status
) VALUES (
    1, '2026-07-06', '右膝', '严重', '治疗中'
);

SELECT id, name, injury_status FROM athlete WHERE id = 1;
```

预期结果：`injury_status` 自动变为 `伤病中`。

软删除回算示例：

```sql
UPDATE injury_record
SET is_deleted = 1, deleted_at = NOW(), deleted_by = 'admin', delete_reason = '误登记'
WHERE id = <某条治疗中记录的id>;

SELECT id, name, injury_status FROM athlete WHERE id = <对应运动员id>;
-- 若无其他活跃伤病，预期变为「健康」
```

---

## 八、权限与安全设计

成员 1 已在 `sql/pingpang_db.sql` 中配置：

| 账号 | 权限 | 用途 |
|---|---|---|
| `admin_app` | `ALL PRIVILEGES` | 管理员、初始化、维护 |
| `coach_app` | `SELECT, INSERT, UPDATE, DELETE` | Flask 业务系统日常访问 |

成员 2 补充：

| 账号 | 权限 | 用途 |
|---|---|---|
| `readonly_app` | `SELECT` | 统计大屏、演示、审计只读访问 |

### 8.1 应用侧推荐用法

| 场景 | 应使用的账号 |
|---|---|
| 运动员/训练/体测/伤病增删改 | `coach_app` |
| 成员 9 统计大屏、只读报表 | `readonly_app` |
| 改表结构、导入初始化脚本 | `admin_app` 或本机 `root` |

设计原则：

- Flask 应用不直接使用 `root` 账号。
- 日常业务使用 `coach_app`，只读展示使用 `readonly_app`。
- 管理员账号 `admin_app` 仅用于数据库维护、初始化和结构调整。
- 生产环境中密码应放入 `.env`，不要写入 Python 源码。
- 库名统一为 `pingpang_db`。

### 8.2 `.env` 示例（供全组对齐）

```env
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=pingpang_db
DB_USER=coach_app
DB_PASSWORD=Coach2026#
DB_READONLY_USER=readonly_app
DB_READONLY_PASSWORD=Readonly2026#
```

---

## 九、备份与恢复方案

### 9.1 完整备份

建议在每次合并到 `dev`、答辩前、数据库结构调整前进行完整备份。

```powershell
mysqldump -u root -p --databases pingpang_db > backup_pingpang_db_20260709.sql
```

### 9.2 仅备份数据

当表结构稳定，只需要保留业务数据时使用：

```powershell
mysqldump -u root -p --no-create-info pingpang_db > backup_pingpang_db_data_20260709.sql
```

### 9.3 恢复数据库

恢复前建议确认当前库是否可覆盖。

```powershell
mysql -u root -p < backup_pingpang_db_20260709.sql
```

如果只恢复数据，需要先执行成员 1 和成员 2 的建库脚本：

```powershell
mysql -u root -p < sql/pingpang_db.sql
mysql -u root -p pingpang_db < sql/member2_advanced_database.sql
mysql -u root -p pingpang_db < backup_pingpang_db_data_20260709.sql
```

### 9.4 备份命名规范

```text
backup_数据库名_日期.sql
backup_pingpang_db_20260709.sql
backup_pingpang_db_before_demo.sql
```

---

## 十、其他成员对接清单

合并本脚本后，建议各模块按下列对象接入，避免继续使用纯内存 Mock：

| 成员 | 建议调用 |
|---|---|
| 成员 3 运动员 | `sp_filter_athletes_by_level`、`v_athlete_comprehensive_profile` |
| 成员 8 伤病 | 依赖 `is_deleted` 等列；状态联动交给触发器 / `sp_refresh_athlete_injury_status` |
| 成员 9 统计 | `v_athlete_comprehensive_profile`、`sp_get_monthly_training_summary`，账号用 `readonly_app` |
| 成员 10 比赛/测试 | `match_record` + 比赛索引；补充触发器与回滚测试 |

---

## 十一、提交与协作说明

成员 2 本次应提交的文件：

```text
sql/member2_advanced_database.sql
docs/成员2_数据库约束安全与高级对象设计.md
```

建议提交信息：

```text
fix: harden member2 injury refresh and complete data dictionary
```

合并目标分支：

```text
dev
```

合并后，成员 3-9 可以直接调用成员 2 提供的视图和存储过程，尤其是：

- `v_athlete_comprehensive_profile`
- `v_monthly_training_summary`
- `sp_filter_athletes_by_level`
- `sp_get_monthly_training_summary`
- `sp_refresh_athlete_injury_status`（含软删除感知）
