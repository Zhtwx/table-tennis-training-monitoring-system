# 乒乓球运动员综合训练监控管理系统前端模板与部署指导

> 当前项目目录中的 `项目要求.txt` 文件为空，因此本框架根据团队提出的 Bootstrap 5、ECharts 5、Flask Jinja2 模板继承、10 人模块分工要求生成。本文档用于统一全组开发环境、依赖安装、运行方式、页面位置和后续扩展规范。

## 1. 已生成文件

```text
app.py                              最小 Flask 启动文件，先占住所有菜单路由
run_server.bat                      Windows 一键启动脚本
requirements.txt                    Python 依赖清单
.env.example                        Docker MySQL 与 Flask 环境变量示例
.gitignore                          忽略虚拟环境、缓存、数据库、上传文件等临时内容
templates/
  base.html                         全站母版，统一导航、侧边栏、资源引入、消息提示
  index.html                        系统主页 / 统计分析大屏，成员9负责
  players/
    list.html                       运动员列表与组合查询，成员3负责
  training/
    batch_import.html               专项技术录入与批量导入，成员6负责
前端模板开发指导.md                  团队开发、部署、协作说明
```

## 2. 每位组员电脑需要安装的东西

### 必装软件

| 软件 | 作用 | 建议 |
|---|---|---|
| Python | 运行 Flask 后端项目 | 建议 Python 3.10 或更高版本 |
| 浏览器 | 预览页面和调试控制台 | Chrome、Edge 均可 |
| VS Code 或 PyCharm | 编写 Flask、HTML、CSS、JS | 任选一种 |
| Git | 团队同步代码 | 如果不使用 Git，可先跳过 |

### VS Code 推荐插件

这些不是项目运行必须项，但建议安装，能明显减少低级错误：

| 插件 | 用途 |
|---|---|
| Python | Python 语法、解释器选择、运行调试 |
| Jinja | Jinja2 模板高亮 |
| HTML CSS Support | HTML/CSS 提示 |
| Bootstrap 5 Quick Snippets | Bootstrap 组件快速提示 |
| JavaScript and TypeScript Nightly | JavaScript 编辑体验增强，可选 |

PyCharm 用户一般不需要额外安装插件，专业版对 Flask/Jinja2 支持更完整；社区版也可以正常编辑和运行。

## 3. Python 依赖说明

项目依赖写在 `requirements.txt` 中。

| 依赖 | 用途 |
|---|---|
| Flask | Web 框架，负责路由、模板渲染、表单接收 |
| Jinja2 | Flask 默认模板引擎，用于模板继承、变量渲染、循环判断 |
| python-dotenv | 读取 `.env` 环境变量，可选但建议保留 |
| openpyxl | 解析 `.xlsx` Excel 文件，成员6批量导入会用到 |
| pandas | 表格数据清洗与统计，可用于 Excel 导入和统计分析 |
| PyMySQL | 连接 Docker Desktop 中的 MySQL 数据库 |

前端库当前通过 CDN 引入，不需要 npm：

| 前端库 | 引入位置 | 用途 |
|---|---|---|
| Bootstrap 5 CSS | `templates/base.html` 头部 | 页面布局、表单、按钮、表格、弹窗 |
| Bootstrap 5 Bundle JS | `templates/base.html` 底部 | Navbar 折叠、Dropdown、Modal 等交互 |
| ECharts 5 | `templates/base.html` 底部 | 统计大屏图表 |

## 4. Windows 部署步骤

以下命令在项目根目录执行，也就是包含 `app.py` 的目录：

```powershell
cd C:\Users\你的用户名\Desktop\乒乓球运动员综合训练监控管理系统
```

创建虚拟环境：

```powershell
py -3 -m venv .venv
```

激活虚拟环境：

```powershell
.\.venv\Scripts\Activate.ps1
```

如果 PowerShell 提示禁止运行脚本，先执行：

```powershell
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
```

然后再次激活：

```powershell
.\.venv\Scripts\Activate.ps1
```

安装依赖：

```powershell
python -m pip install --upgrade pip
python -m pip install -r requirements.txt
```

启动项目：

```powershell
python app.py
```

浏览器打开：

```text
http://127.0.0.1:5000
```

默认登录账号：

| 身份 | 用户名 | 密码 | 权限范围 |
|---|---|---|---|
| 管理员 | `admin` | `admin123` | 可访问全部模块，包括用户权限和系统配置 |
| 普通用户 | `coach` | `user123` | 可访问训练、运动员、体能、伤病、康复、比赛等业务模块 |

Windows 也可以直接双击项目根目录下的 `run_server.bat` 启动。该脚本固定使用当前项目自己的 `.venv\Scripts\python.exe`，避免误调用系统 Python。注意：如果体育器材旧项目还在运行，它会占用 `5000` 端口，需要先在旧项目终端按 `Ctrl + C` 停掉。

本机已从临时复制的体育器材项目虚拟环境中学习并补齐当前项目 `.venv` 的 Flask 运行包。确认当前项目能启动后，可以删除 `体育器材管理系统源代码` 文件夹；当前项目不再依赖它。

## 5. Docker Desktop MySQL 配置

从当前 Docker Desktop 截图看，你的电脑里有两个正在运行的 MySQL 容器：

| 容器名 | 镜像 | 本机访问端口 | 说明 |
|---|---|---|---|
| `my-mysql-55` | `mysql:5.6` | `127.0.0.1:3306` | 适合兼容旧项目或旧 SQL 语法 |
| `my-mysql-8` | `mysql:8.0` | `127.0.0.1:3307` | 推荐新项目优先使用 |

当前前端毛坯页面暂时不强制连接数据库，所以只要 Flask 能启动，页面就可以打开。等成员开始做运动员、训练、伤病等真实增删改查时，再接入 MySQL。

建议复制 `.env.example` 为 `.env`，然后按实际选择的容器修改：

```text
DB_HOST=127.0.0.1
DB_PORT=3307
DB_USER=root
DB_PASSWORD=你的MySQL密码
DB_NAME=table_tennis_training
```

如果使用截图里的 `my-mysql-8`，端口写 `3307`。如果使用 `my-mysql-55`，端口写 `3306`。

后续 Flask 读取配置时推荐这样写：

```python
import os
from dotenv import load_dotenv

load_dotenv()

DB_CONFIG = {
    "host": os.getenv("DB_HOST", "127.0.0.1"),
    "port": int(os.getenv("DB_PORT", "3306")),
    "user": os.getenv("DB_USER", "root"),
    "password": os.getenv("DB_PASSWORD", ""),
    "database": os.getenv("DB_NAME", "table_tennis_training"),
    "charset": "utf8mb4",
}
```

连接测试示例：

```python
import pymysql

conn = pymysql.connect(**DB_CONFIG)
with conn.cursor() as cursor:
    cursor.execute("SELECT VERSION()")
    print(cursor.fetchone())
conn.close()
```

## 6. macOS / Linux 部署步骤

进入项目目录：

```bash
cd /你的路径/乒乓球运动员综合训练监控管理系统
```

创建虚拟环境：

```bash
python3 -m venv .venv
```

激活虚拟环境：

```bash
source .venv/bin/activate
```

安装依赖：

```bash
python -m pip install --upgrade pip
python -m pip install -r requirements.txt
```

启动项目：

```bash
python app.py
```

浏览器打开：

```text
http://127.0.0.1:5000
```

## 7. 离线部署 Bootstrap 和 ECharts

当前模板使用 CDN，优点是不用下载前端包；缺点是断网时样式和图表可能加载失败。如果教室或答辩环境不能联网，按下面方式改成本地资源。

建议目录：

```text
static/
  vendor/
    bootstrap/
      bootstrap.min.css
      bootstrap.bundle.min.js
    echarts/
      echarts.min.js
```

需要下载的文件：

```text
bootstrap.min.css
bootstrap.bundle.min.js
echarts.min.js
```

然后把 `templates/base.html` 中的 CDN 链接替换为本地路径。

CSS 替换为：

```jinja2
<link rel="stylesheet" href="{{ url_for('static', filename='vendor/bootstrap/bootstrap.min.css') }}">
```

Bootstrap JS 替换为：

```jinja2
<script src="{{ url_for('static', filename='vendor/bootstrap/bootstrap.bundle.min.js') }}"></script>
```

ECharts JS 替换为：

```jinja2
<script src="{{ url_for('static', filename='vendor/echarts/echarts.min.js') }}"></script>
```

注意：如果使用 CDN，部署电脑必须能访问外网；如果使用本地静态文件，项目目录中必须真的存在这些文件。

## 8. 全站模板继承规范

所有业务页面都必须继承 `templates/base.html`，不要在子页面重复写 `<html>`、`<head>`、`<body>`。

标准写法：

```jinja2
{% extends "base.html" %}

{% block title %}页面标题 - 乒乓球训练监控管理系统{% endblock %}

{% block content %}
页面主体内容写在这里
{% endblock %}

{% block scripts %}
页面专属 JavaScript 写在这里
{% endblock %}
```

`base.html` 已统一完成：

- Bootstrap 5 CSS 引入
- Bootstrap 5 Bundle JS 引入
- ECharts 5 JS 引入
- 顶部响应式导航栏
- 左侧 10 人团队模块菜单
- Flask `flash()` 消息展示区
- `{% block content %}` 页面内容插槽
- `{% block scripts %}` 页面脚本插槽

## 9. 后端路由 endpoint 对照表

`base.html` 中预留了 `url_for()` 链接。`app.py` 已经先创建最小路由，保证模板可以打开。后续成员开发时，不要随意改 endpoint 名称，否则菜单链接会失效。

| 成员 | 模块 | endpoint | 当前状态 | 建议模板位置 |
|---|---|---|---|---|
| 成员1 | 用户权限管理 | `auth.users` | 占位 | `templates/auth/users.html` |
| 成员2 | 系统字典配置 | `settings.dictionary` | 占位 | `templates/settings/dictionary.html` |
| 成员3 | 运动员档案与组合查询 | `players.list` | 已有页面 | `templates/players/list.html` |
| 成员4 | 训练计划管理 | `training.plans` | 占位 | `templates/training/plans.html` |
| 成员5 | 体能测试评估 | `fitness.tests` | 占位 | `templates/fitness/tests.html` |
| 成员6 | 专项技术录入与批量导入 | `training.batch_import` | 已有页面 | `templates/training/batch_import.html` |
| 成员7 | 伤病记录管理 | `injuries.list` | 占位 | `templates/injuries/list.html` |
| 成员8 | 康复跟踪预警 | `rehab.list` | 占位 | `templates/rehab/list.html` |
| 成员9 | 统计分析大屏 | `index` | 已有页面 | `templates/index.html` |
| 成员10 | 比赛成绩报告 | `matches.list` | 占位 | `templates/matches/list.html` |

成员3页面额外使用：

- `players.create`：新增运动员
- `players.edit`：编辑运动员，参数 `player_id`
- `injuries.history`：查看某位运动员历史伤病，参数 `player_id`

成员6页面额外使用：

- `training.import_excel`：Excel 批量导入接口

## 10. 成员9：统计大屏开发说明

负责文件：`templates/index.html`

当前页面已经包含：

- 4 个概览指标卡片
- “全队伤病分布图”ECharts 饼图
- “月度训练时长趋势”ECharts 折线图
- Mock 数据初始化脚本

后续替换真实数据有两种方式。

方式一：Flask 模板变量。

```python
@app.route("/")
def index():
    injury_data = [
        {"value": 3, "name": "肩部"},
        {"value": 2, "name": "腰部"},
    ]
    monthly_hours = [168, 192, 214, 238, 226, 252]
    return render_template("index.html", injury_data=injury_data, monthly_hours=monthly_hours)
```

模板中替换：

```javascript
const injuryData = {{ injury_data | tojson }};
const monthlyHours = {{ monthly_hours | tojson }};
```

方式二：异步接口。

```javascript
fetch('/api/statistics/injuries')
    .then(response => response.json())
    .then(data => {
        injuryChart.setOption({ series: [{ data: data }] });
    });
```

## 11. 成员3：运动员列表与组合查询开发说明

负责文件：`templates/players/list.html`

当前页面已经包含：

- 学号 / 编号输入框，支持模糊匹配
- 姓名输入框，支持模糊匹配
- 打法关键词输入框，支持模糊匹配
- 性别下拉框
- 运动等级下拉框
- 伤病状态下拉框
- 年龄范围输入框
- AND / OR 条件逻辑选择
- 查询按钮
- Bootstrap 5 数据表格
- 编辑、删除、查看历史伤病按钮
- 删除确认弹窗

老师补充要求已经落实到页面和后端：不能只做单字段查询，必须支持任意多个条件随机组合。文本类字段必须支持模糊查询，例如学号包含、姓名包含、打法关键词包含；等级、性别、伤病状态可作为精确条件参与组合；年龄使用范围筛选。默认逻辑为 AND，也支持 OR。

后端建议：

```python
@players_bp.route("/players", endpoint="list")
def list_players():
    student_no = request.args.get("student_no", "")
    name = request.args.get("name", "")
    play_style = request.args.get("play_style", "")
    gender = request.args.get("gender", "")
    level = request.args.get("level", "")
    injury_status = request.args.get("injury_status", "")
    age_min = request.args.get("age_min", "")
    age_max = request.args.get("age_max", "")
    logic = request.args.get("logic", "and")
    players = query_players(
        student_no=student_no,
        name=name,
        play_style=play_style,
        gender=gender,
        level=level,
        injury_status=injury_status,
        age_min=age_min,
        age_max=age_max,
        logic=logic,
    )
    return render_template("players/list.html", players=players)
```

数据库接入时，AND 模式可以生成类似 SQL：

```sql
WHERE student_no LIKE '%2026%'
  AND name LIKE '%王%'
  AND play_style LIKE '%横板%'
  AND level = 'first'
  AND injury_status = 'healthy'
  AND age >= 18
  AND age <= 22
```

OR 模式可以把连接词换成 `OR`。不要只实现 `WHERE student_no = ?` 或 `WHERE name = ?` 这种单字段精确查询。

表格正式接入时，把 Mock 行替换为：

```jinja2
{% for player in players %}
<tr>
    <td>{{ player.id }}</td>
    <td>{{ player.name }}</td>
    <td>{{ player.gender }}</td>
    <td>{{ player.age }}</td>
    <td>{{ player.level }}</td>
    <td>{{ player.play_style }}</td>
    <td>{{ player.injury_status }}</td>
    <td class="text-end">操作按钮</td>
</tr>
{% else %}
<tr>
    <td colspan="8" class="text-center text-secondary py-4">暂无运动员数据</td>
</tr>
{% endfor %}
```

## 12. 成员6：专项技术录入与批量导入开发说明

负责文件：`templates/training/batch_import.html`

当前页面已经包含：

- 运动员选择
- 训练日期
- 步法训练
- 击球技术
- 多球训练时长
- 训练强度
- 训练备注
- Excel 文件上传
- 数据校验 Alert
- 数据异常 Modal

日常录入后端建议：

```python
@training_bp.route("/training/batch-import", methods=["GET", "POST"], endpoint="batch_import")
def batch_import():
    if request.method == "POST":
        player_id = request.form.get("player_id")
        training_date = request.form.get("training_date")
        footwork_type = request.form.get("footwork_type")
        stroke_technique = request.form.get("stroke_technique")
        multi_ball_minutes = request.form.get("multi_ball_minutes")
        intensity = request.form.get("intensity")
        training_note = request.form.get("training_note")
        # 校验并保存数据库
        flash("训练记录保存成功", "success")
        return redirect(url_for("training.batch_import"))
    return render_template("training/batch_import.html")
```

Excel 上传后端建议：

```python
@training_bp.route("/training/import-excel", methods=["POST"], endpoint="import_excel")
def import_excel():
    file = request.files.get("training_excel")
    if not file:
        flash("请先选择 Excel 文件", "warning")
        return redirect(url_for("training.batch_import"))
    # 使用 openpyxl 或 pandas 解析、校验、入库
    flash("Excel 导入成功", "success")
    return redirect(url_for("training.batch_import"))
```

## 13. 常见问题

### 页面内容要面向谁

系统页面必须按“客户实际使用界面”来写，不能在页面上出现“成员负责”“Mock 数据”“接口替换”“预留位置”“毛坯演示”等开发提示。所有分工、接口说明、Jinja2 循环替换说明、数据库字段说明，统一写在本文档或后续技术文档中。

### 运行时报 `No module named flask`

说明依赖没装好，先确认虚拟环境已经激活，再执行：

```bash
python -m pip install -r requirements.txt
```

### 运行时报 `BuildError`

通常是模板中的 `url_for('某个endpoint')` 找不到对应路由。当前 `app.py` 已经占住主要路由；如果你新增了菜单链接，就必须同时新增对应 Flask 路由。

### 页面没有样式

如果使用 CDN，检查电脑是否联网。如果无法联网，按“离线部署 Bootstrap 和 ECharts”章节改成本地静态文件。

### ECharts 图表不显示

常见原因：

- 浏览器无法加载 `echarts.min.js`
- 图表容器没有高度
- 页面脚本执行时容器还没渲染完成

当前 `index.html` 已经使用 `DOMContentLoaded` 并给 `.chart-box` 设置了高度。

### Excel 上传后后端收不到文件

文件上传表单必须包含：

```html
enctype="multipart/form-data"
```

后端获取文件：

```python
file = request.files.get("training_excel")
```

### Docker Desktop 里 MySQL 是运行的，但 Flask 还是打不开

MySQL 只负责数据库，不能替代 Flask。页面打不开通常优先检查：

- 当前 Python 是否安装 Flask
- `5000` 端口是否被其他项目占用
- `python app.py` 是否已经启动成功

数据库连接问题一般会在做增删改查时出现，不会影响当前静态毛坯页面显示。

## 14. 视觉与协作规范

- 子页面统一使用 `container`、`row`、`col-*`、`card`、`shadow-sm`、`btn-primary` 等 Bootstrap 5 标准类。
- 页面主标题使用 `page-title h3`。
- 表单必须有 `label`，输入项必须有稳定的 `id` 和 `name`。
- 表格外层使用 `.table-responsive`，保证小屏幕可横向滚动。
- 图表容器必须有固定高度，例如 `.chart-box { height: 360px; }`。
- 弹窗统一使用 Bootstrap Modal，不要自己写复杂弹窗样式。
- 页面专属脚本写入 `{% block scripts %}`，不要写到 `base.html` 中。
- 每位成员只改自己负责的模板、路由和数据处理逻辑，公共母版 `base.html` 如需修改应先和组长确认。

## 15. 新成员开发页面的最小步骤

1. 在 `templates/模块名/页面名.html` 新建模板。
2. 第一行写 `{% extends "base.html" %}`。
3. 在 `{% block content %}` 中写页面主体。
4. 在 Flask 中创建对应路由，并确保 endpoint 与 `base.html` 菜单中的 `url_for()` 名称一致。
5. 打开页面检查导航栏、侧边栏、Flash 消息、响应式布局是否正常。

## 16. 提交前检查清单

- 页面能通过 `python app.py` 正常打开。
- 浏览器控制台没有明显 JavaScript 报错。
- 表单字段有 `name`，后端能通过 `request.form` 或 `request.args` 获取。
- 页面没有重复写 `<html>`、`<head>`、`<body>`。
- 新增菜单链接时，同步新增 Flask 路由。
- 如果引入新 Python 库，同步写入 `requirements.txt`。
- 如果引入新静态文件，放入 `static/` 目录，不要散放在项目根目录。
- 不要提交 `.venv/`、`__pycache__/`、`.env`、数据库文件、上传文件，`.gitignore` 已经做了默认忽略。
