# 乒乓球运动员综合训练监控管理系统部署指导手册

本文档用于团队成员在自己的电脑上部署、启动和预览《乒乓球运动员综合训练监控管理系统》。请按步骤操作，不要跳步。

## 1. 项目说明

本项目是一个基于 Flask 的 Web 管理系统，前端使用 Bootstrap 5 和 ECharts 5，后端使用 Flask Jinja2 模板继承。

当前系统已包含：

- 独立登录页
- 管理员 / 普通用户角色区分
- 登录状态保护
- 退出登录
- 权限拦截页面
- 综合训练看板
- 运动员档案页面
- 专项技术录入与 Excel 批量导入页面
- 用户权限管理页面
- 训练计划、体能测试、伤病记录、康复跟踪、比赛成绩、系统配置等模块入口

## 2. 项目目录结构

项目根目录中主要文件如下：

```text
乒乓球运动员综合训练监控管理系统/
  app.py                              Flask 主程序
  run_server.bat                      Windows 一键启动脚本
  requirements.txt                    Python 依赖清单
  .env.example                        数据库与系统环境变量示例
  .gitignore                          Git 忽略规则
  前端模板开发指导.md                  开发分工与模板说明
  项目部署指导手册.md                  本部署手册
  templates/
    base.html                         全站母版
    index.html                        综合训练看板
    module_overview.html              通用模块概览页
    auth/
      login.html                      登录页
      forbidden.html                  无权限页面
      users.html                      用户权限管理页
    players/
      list.html                       运动员档案页
    training/
      batch_import.html               专项技术录入与批量导入页
```

以下目录不用手动修改：

```text
.venv/
__pycache__/
.codex_tmp/
.agents/
.git/
```

## 3. 每台电脑需要安装的软件

### 必装软件

| 软件 | 用途 | 建议 |
|---|---|---|
| Python | 运行 Flask 项目 | 建议 Python 3.10 或 3.11 |
| 浏览器 | 访问系统页面 | Chrome / Edge 均可 |
| VS Code 或 PyCharm | 编写和查看代码 | 任选 |

### 可选软件

| 软件 | 用途 |
|---|---|
| Git | 团队代码同步 |
| Docker Desktop | 运行 MySQL 数据库 |
| MySQL Workbench / DBeaver | 查看数据库 |

### VS Code 推荐插件

| 插件 | 用途 |
|---|---|
| Python | Python 语法和运行支持 |
| Jinja | Jinja2 模板高亮 |
| HTML CSS Support | HTML / CSS 提示 |
| Bootstrap 5 Quick Snippets | Bootstrap 组件提示 |

## 4. Windows 快速启动方式

如果项目文件夹中已经存在 `.venv`，优先使用此方式。

1. 打开项目根目录：

```text
C:\Users\你的用户名\Desktop\乒乓球运动员综合训练监控管理系统
```

2. 双击：

```text
run_server.bat
```

3. 看到类似以下内容表示启动成功：

```text
Starting Table Tennis Training Monitoring System...
Web URL: http://localhost:5000
 * Running on http://127.0.0.1:5000
```

4. 浏览器打开：

```text
http://localhost:5000
```

5. 进入登录页后使用默认账号登录。

## 5. 默认登录账号

| 身份 | 用户名 | 密码 | 权限范围 |
|---|---|---|---|
| 管理员 | `admin` | `admin123` | 可访问全部模块，包括用户权限、系统配置、新增/编辑/删除运动员 |
| 普通用户 | `coach` | `user123` | 可访问训练、运动员、体能、伤病、康复、比赛等业务模块 |

管理员登录后可以看到：

- 用户权限
- 系统配置
- 新增运动员
- 编辑运动员
- 删除运动员

普通用户登录后不会看到管理员专属功能；如果直接访问管理员地址，系统会显示 403 无权限页面。

## 6. 从零部署方式

如果你的电脑第一次运行本项目，或没有 `.venv`，按下面步骤操作。

### 6.1 打开终端

在项目文件夹空白处右键，选择：

```text
在终端中打开
```

或打开 PowerShell 后进入项目目录：

```powershell
cd "C:\Users\你的用户名\Desktop\乒乓球运动员综合训练监控管理系统"
```

### 6.2 创建虚拟环境

```powershell
python -m venv .venv
```

如果你的电脑使用 `py` 启动 Python，也可以执行：

```powershell
py -3 -m venv .venv
```

### 6.3 激活虚拟环境

PowerShell：

```powershell
.\.venv\Scripts\Activate.ps1
```

CMD：

```bat
.\.venv\Scripts\activate.bat
```

如果 PowerShell 提示禁止运行脚本，先执行：

```powershell
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
```

然后再次执行：

```powershell
.\.venv\Scripts\Activate.ps1
```

### 6.4 安装依赖

```powershell
python -m pip install --upgrade pip
python -m pip install -r requirements.txt
```

### 6.5 启动项目

```powershell
python app.py
```

或直接双击：

```text
run_server.bat
```

### 6.6 打开系统

```text
http://localhost:5000
```

## 7. macOS / Linux 部署方式

进入项目目录：

```bash
cd /你的路径/乒乓球运动员综合训练监控管理系统
```

创建虚拟环境：

```bash
python3 -m venv .venv
```

激活虚拟环境：

```bash
source .venv/bin/activate
```

安装依赖：

```bash
python -m pip install --upgrade pip
python -m pip install -r requirements.txt
```

启动项目：

```bash
python app.py
```

浏览器打开：

```text
http://localhost:5000
```

## 8. requirements.txt 依赖说明

当前依赖如下：

```text
Flask
Jinja2
python-dotenv
openpyxl
pandas
PyMySQL
```

用途说明：

| 依赖 | 用途 |
|---|---|
| Flask | Web 后端框架 |
| Jinja2 | 模板引擎 |
| python-dotenv | 读取 `.env` 配置 |
| openpyxl | 读取 Excel 文件 |
| pandas | 表格数据处理 |
| PyMySQL | 连接 MySQL 数据库 |

## 9. Docker Desktop MySQL 配置

如果使用 Docker Desktop 运行 MySQL，当前推荐使用下面两种方式之一。

### 9.1 已有 Docker 容器

如果你的 Docker Desktop 中已经有 MySQL 容器，请确认端口：

| 容器 | 本机端口 | 容器端口 | 访问地址 |
|---|---|---|---|
| MySQL 5.x | `3306` | `3306` | `127.0.0.1:3306` |
| MySQL 8.x | `3307` | `3306` | `127.0.0.1:3307` |

截图中的示例：

```text
my-mysql-55   3306:3306
my-mysql-8    3307:3306
```

如果使用 `my-mysql-8`，数据库端口应填写：

```text
DB_PORT=3307
```

如果使用 `my-mysql-55`，数据库端口应填写：

```text
DB_PORT=3306
```

### 9.2 新建 MySQL 8 容器

如果电脑上没有 MySQL 容器，可以在 Docker Desktop 正常运行后执行：

```powershell
docker run --name table-tennis-mysql -p 3307:3306 -e MYSQL_ROOT_PASSWORD=123456 -d mysql:8.0
```

说明：

```text
容器名：table-tennis-mysql
本机端口：3307
MySQL 用户名：root
MySQL 密码：123456
```

## 10. .env 配置

项目中提供了 `.env.example`。正式使用时复制一份并改名为：

```text
.env
```

示例内容：

```text
FLASK_ENV=development
SECRET_KEY=change-this-secret-key

DB_HOST=127.0.0.1
DB_PORT=3307
DB_USER=root
DB_PASSWORD=123456
DB_NAME=table_tennis_training
```

注意：

- `.env.example` 可以提交给组员。
- `.env` 是本机私有配置，不建议提交。
- 当前系统页面可以不连接 MySQL 直接预览。
- 后续做真实增删改查时再接入数据库。

## 11. 前端资源说明

当前项目使用 CDN 引入：

```text
Bootstrap 5
ECharts 5
```

所以预览页面时需要能访问外网。如果答辩环境或教室网络不能访问 CDN，需要改成本地静态资源。

建议目录：

```text
static/
  vendor/
    bootstrap/
      bootstrap.min.css
      bootstrap.bundle.min.js
    echarts/
      echarts.min.js
```

然后修改 `templates/base.html` 中的资源引用。

Bootstrap CSS：

```jinja2
<link rel="stylesheet" href="{{ url_for('static', filename='vendor/bootstrap/bootstrap.min.css') }}">
```

Bootstrap JS：

```jinja2
<script src="{{ url_for('static', filename='vendor/bootstrap/bootstrap.bundle.min.js') }}"></script>
```

ECharts：

```jinja2
<script src="{{ url_for('static', filename='vendor/echarts/echarts.min.js') }}"></script>
```

## 12. 端口占用处理

Flask 默认使用：

```text
5000
```

如果启动时报端口已占用，通常是旧项目还在运行。

解决方式：

1. 找到之前启动 Flask 的黑色终端窗口。
2. 按：

```text
Ctrl + C
```

3. 重新启动当前项目。

如果需要临时换端口，可以在 `app.py` 最后一行改为：

```python
app.run(debug=True, use_reloader=False, port=5001)
```

然后访问：

```text
http://localhost:5001
```

## 13. 常见问题

### 13.1 页面显示“localhost 拒绝连接”

原因：Flask 服务没有成功启动。

检查：

- 是否双击了 `run_server.bat`
- 终端是否显示 `Running on http://127.0.0.1:5000`
- 是否有报错
- 5000 端口是否被其他项目占用

### 13.2 报错 `No module named flask`

原因：当前 Python 环境没有安装 Flask。

解决：

```powershell
python -m pip install -r requirements.txt
```

如果项目中已有 `.venv`，可以执行：

```powershell
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
```

### 13.3 双击 bat 后中文乱码

不影响系统运行。当前 `run_server.bat` 已尽量使用英文提示，避免 CMD 编码问题。

### 13.4 登录后访问用户权限显示 403

原因：当前账号不是管理员。

请使用管理员账号：

```text
admin / admin123
```

### 13.5 普通用户看不到新增、编辑、删除按钮

这是正常权限控制。

普通用户账号：

```text
coach / user123
```

只能访问业务模块，不能进行管理员操作。

### 13.6 页面没有样式或图表不显示

可能原因：

- 无法访问 Bootstrap CDN
- 无法访问 ECharts CDN
- 浏览器网络受限

解决：

- 确认电脑能联网
- 或按“前端资源说明”改成本地静态资源

### 13.7 Docker Desktop 里 MySQL 是运行的，但页面打不开

MySQL 只负责数据库，不负责启动网页。

页面打不开优先检查：

- Flask 是否启动
- Python 是否安装依赖
- 5000 端口是否被占用

数据库连接问题通常只会影响后续真实数据增删改查。

## 14. 启动验证清单

启动成功后，依次检查：

1. 打开 `http://localhost:5000`
2. 自动进入登录页
3. 使用 `admin / admin123` 登录
4. 能看到综合看板
5. 能进入用户权限页面
6. 退出登录
7. 使用 `coach / user123` 登录
8. 普通用户看不到用户权限和系统配置入口
9. 普通用户直接访问 `/auth/users` 显示 403

## 15. 多条件模糊组合查询验收要求

本次作业明确要求不能只做简单查询。只输入一个学号查表、只输入一个姓名查表、只提供一个下拉框单选条件，都不算合格的多条件模糊查询。

运动员档案页已经实现多条件随机组合查询，访问路径：

```text
http://localhost:5000/players/
```

当前页面支持同时填写：

- 学号 / 编号：支持模糊匹配，例如输入 `2026` 可匹配所有 `2026` 开头编号。
- 姓名：支持模糊匹配，例如输入 `王` 可匹配 `王一鸣`。
- 打法关键词：支持模糊匹配，例如输入 `横板`、`弧圈`、`快攻`。
- 性别：可参与组合筛选。
- 运动等级：可参与组合筛选。
- 伤病状态：可参与组合筛选。
- 最小年龄 / 最大年龄：支持范围筛选。
- 条件逻辑：支持 `AND 同时满足` 和 `OR 任一满足`。

推荐验收测试：

```text
学号输入：2026
姓名输入：王
打法关键词输入：横板
运动等级选择：一级运动员
伤病状态选择：健康
条件逻辑选择：AND 同时满足
```

这表示系统会查找“学号包含 2026，姓名包含王，打法包含横板，等级为一级运动员，并且伤病状态为健康”的运动员。

也可以测试：

```text
姓名输入：李
打法关键词输入：快攻
条件逻辑选择：OR 任一满足
```

这表示系统会查找“姓名包含李，或者打法包含快攻”的运动员。

后续接入数据库时，SQL 条件应按多个字段动态拼接，例如：

```sql
WHERE student_no LIKE '%2026%'
  AND name LIKE '%王%'
  AND play_style LIKE '%横板%'
  AND level = 'first'
  AND injury_status = 'healthy'
```

不能写成只查一个字段的形式，例如：

```sql
WHERE student_no = '2026001'
```

或：

```sql
WHERE name = '王一鸣'
```

这种单字段精确查询不满足本次作业要求。

## 16. 团队协作规则

### 16.1 页面要面向客户

页面中不能出现：

```text
成员负责
Mock 数据
预留位置
开发说明
接口替换
毛坯演示
等待成员补充
```

这些内容只能写在开发文档里，不能显示给客户。

### 16.2 开发说明写在哪里

开发说明统一写入：

```text
前端模板开发指导.md
```

或新建其他 `.md` 文档。

### 16.3 每个成员改哪里

| 模块 | 建议文件 |
|---|---|
| 登录和权限 | `app.py`、`templates/auth/` |
| 综合看板 | `templates/index.html` |
| 运动员档案 | `templates/players/list.html` |
| 专项技术录入 | `templates/training/batch_import.html` |
| 训练计划 | 可基于 `templates/module_overview.html` 扩展 |
| 伤病记录 | 可基于 `templates/module_overview.html` 扩展 |
| 体能测试 | 可基于 `templates/module_overview.html` 扩展 |
| 康复跟踪 | 可基于 `templates/module_overview.html` 扩展 |
| 比赛成绩 | 可基于 `templates/module_overview.html` 扩展 |
| 系统配置 | 可基于 `templates/module_overview.html` 扩展 |

### 16.4 不要提交的内容

不要提交：

```text
.venv/
__pycache__/
.env
*.db
*.sqlite
uploads/
.codex_tmp/
```

这些内容已经写入 `.gitignore`。

## 17. 当前版本说明

当前版本是可独立运行的系统框架，重点完成：

- 统一 UI 风格
- 登录认证
- 角色权限
- 导航体系
- 核心业务页面
- 图表看板
- Excel 上传入口
- Docker MySQL 配置说明

后续成员需要继续完成：

- 数据库表结构
- 真实用户表
- 运动员数据表
- 训练记录表
- 伤病记录表
- Excel 解析入库
- ECharts 接入真实接口数据
- 管理员新增、编辑、删除真实业务逻辑
